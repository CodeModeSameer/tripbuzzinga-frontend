export const metadata = {
  title: 'Admin Panel - Trip Buzzinga',
  description: 'Manage Trip Buzzinga Content',
};

export default function AdminLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
